#!/usr/bin/env bash
# Wired by `just setup` (core.hooksPath = .githooks); UNIVERSE_SKIP_HOOKS=1 skips it for one push.
set -euo pipefail
[ "${UNIVERSE_SKIP_HOOKS:-}" = 1 ] && exit 0
cd "$(git rev-parse --show-toplevel)"
just verify
