[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "universe-core"
dynamic = ["version"]
description = "Python bindings to the Universe core"
requires-python = ">=3.11"

[tool.maturin]
module-name = "universe_core"
